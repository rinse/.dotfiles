[core]
	editor = vim

[color]
	ui = auto

[alias]
	hash = log --format='%h'
	graph = log --graph --date=short --decorate=short --pretty=format:'%Cgreen%h %Creset%cd %Cblue%cn %Cred%d %Creset%s'
	current-branch = rev-parse --abbrev-ref HEAD
	sdiff = diff --ignore-space-change

[push]
	default = simple

[diff]
	compactionHeuristic = true
